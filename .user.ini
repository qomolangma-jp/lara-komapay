; Laravel用のPHP設定
; open_basedir制限を完全に解除（本番サーバー用）
; open_basedir = none

; メモリ制限
memory_limit = 256M

; アップロードサイズ
upload_max_filesize = 20M
post_max_size = 25M

; 実行時間
max_execution_time = 300
max_input_time = 300
